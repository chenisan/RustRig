/target

# ASIO SDK（Steinberg 專有授權，不可進 repo；本機自用，CPAL_ASIO_DIR 指向它）
/third_party/

# 本機專屬建置設定（含 machine-specific 路徑：CPAL_ASIO_DIR / LIBCLANG_PATH）
/.cargo/config.toml

# 打包輸出
/dist/

# 下載的 NAM 模型（各自授權，自備、不轉散）
/models/
